spring:
  application:
    name: audiogpt
  ai:
    ollama:
      base-url: http://localhost:11434
      chat:
        model: qwen3:8b

  datasource:
    url: jdbc:postgresql://localhost:5432/audiogpt
    username: postgres
    password: ${DB_PWD}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
